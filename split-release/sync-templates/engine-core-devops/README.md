# Engine core-devops templates (public repo)

These files are **templates only** for private-repository workflows and for
`split-release/prepare-karma2-sync-package.sh`.

The full `karma-engine` tree is **not** stored in this public repository.
