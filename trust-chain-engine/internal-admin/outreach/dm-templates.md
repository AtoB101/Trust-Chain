# Trust-Chain Seller Outreach DM Templates (Private)

Use these first-touch messages for seller segments.

## Core pitch (all segments)

一句话：让你的 Agent/工具每次被调用都自动收费，链上可验证，用户无需预存到托管账户。

## Segment templates

### TelegramSignalAdmin
你好，我们在做一个 Agent 按调用自动收费网关。你的信号可以做到“每次触发就收费并留链上证据”，可以防白嫖和纠纷。愿意给你一个 10 分钟 demo 看看吗？

### PrivateStrategySeller
你好，你的私域策略现在主要靠订阅制。我们这边可以做“按次调用收费”，比如每次策略触发/查询都自动结算，支持争议与证据留存。愿不愿意试一版最小接入？

### AutoTradingBotDev
你好，你的 Bot 很适合做调用级收费。我们可以让 Bot 的每次价格查询/信号调用自动扣费并上链留证，接入侧只要 wrap 一层。可否约个时间给你跑一次首单？

### WhaleAlertBotDev
你好，你的钱包异动监控能力很有商业化空间。我们支持按调用收费 + 自动结算 + tx 证明，能直接变成可计费 API。愿不愿意做首批合作测试？

### SmartMoneyTracker
你好，Smart Money 数据天然高频。我们支持“每次查询即收费”，并把结算和证据自动化。你只需要接一个轻量包装器就能开始收款。感兴趣吗？

### OnchainArbScanner
你好，你的套利扫描结果价值高，按调用收费会更匹配。我们提供非托管结算路径，避免人工催费。愿不愿意跑一个 1 天试点？

### NftSmartMoneyWatcher
你好，NFT 资金流监控可直接商品化。我们可以让每次查询自动计费并链上可审计，适合你的目标用户。要不要一起做一个最小演示？

## A/R/D/C generic templates

### A class generic
你好，我们在做 Agent API 按调用自动收费。你这类数据工具非常适合“调用即结算”的模式，可以快速验证收入。愿意做一次接入测试吗？

### R class generic
你好，你的安全/风控能力付费意愿通常更强。我们支持按次收费和争议证据，对安全检测非常匹配。愿不愿意先跑一个小流量试点？

### D class generic
你好，你的数据资产很适合做按查询收费。我们可提供调用级计费与结算证明，先从最小 SKU 开始。是否有兴趣试接入？

### C class generic
你好，我们在做加密信息工具的调用收费网关。你可把现有抓取/舆情能力快速转成计费 API，支持自动结算。愿意试一个 demo 吗？

